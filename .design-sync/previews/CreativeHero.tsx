import { CreativeHero } from 'adrianeddy-portfolio'

// Prop-less opener for /creative: back-link / volume / byline meta row,
// oversized serif "Creative." headline, centered serif intro. One canonical
// render. (The back link works because previews are wrapped in PreviewRouter.)
export function Default() {
    return <CreativeHero />
}
