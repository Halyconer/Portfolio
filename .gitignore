node_modules/
dist/
# Pi runtime data (generated JSON, sqlite, logs) — served, never committed
/data/
.env
.env.local
.env*.local
*.pyc
__pycache__/
refresh_token
.venv/
**/.DS_Store
.vscode/
/assets/

# Machine-local personal context for AI sessions — never commit
.opencode/personal.md

# Full-res creative photo masters (256MB+) — live on Cloudflare R2, never commit
/masters/
# opencode machine-local state (regenerated on any machine)
.opencode/node_modules/
.opencode/package.json
.opencode/package-lock.json
.opencode/bun.lock

# Playwright MCP artifacts (page snapshots, traces) from local browser sessions
.playwright-mcp/
